.. code:: robotframework
	*** Keywords ***
	The result of ${live} should contain ${die}
		${live} = World 

	Create nature
	
	Create builder
	
	Create world 
		[Arguments]	${nature}	${builder}
		${world} = World ${nature} ${builder}
	
	World live should contain ${world}
		${world}.operation 
	
	*** Variables ***
	${NATURE}	Nature()
	${BUILDER}	CellBuilder()
	
	*** Test Cases ***
	Test the world	
		The result of live should contain Bacterial cell dies


	
